[pytest]
DJANGO_SETTINGS_MODULE = plio.test_settings
python_files = tests.py test_*.py
markers =
    integration: backend API journeys through the real Django app
    slow_lane: tests requiring committed database transactions
